{
    # ログなどを置くディレクトリを指定する。必須。
    log     => +{
        dir             => '/your/log/dir/rej',
    },
    # AbEnd 時に送るメールについての設定。
    mail    => +{
        send            => 1,
        to              => 'to@email.address',
        from            => 'from@email.address',
        subject         => '',
        hist_log_size   => 100,
    },
}
